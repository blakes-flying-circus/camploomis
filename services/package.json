{
  "name": "camploomis-services",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./build/proto && mkdir -p ./build/proto",
    "build": "sh scripts/ts_build.sh"
  },
  "devDependencies": {
    "grpc-tools": "^1.11.2",
    "grpc_tools_node_protoc_ts": "^5.3.2",
    "prettier": "^2.5.0"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100,
    "endOfLine": "auto"
  },
  "dependencies": {
    "globalthis": "^1.0.2"
  }
}
