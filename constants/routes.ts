export const API_LOGOUT = `/api/auth/logout` as const;
export const API_LOGIN = `/api/auth/login` as const;
export const API_RECENT_BEERS = '/api/recent-beers' as const;

export const ADMIN_HOME = `/admin`;
